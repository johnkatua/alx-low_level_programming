#include <stdio.h>
/**
 * main - Entry point
 * Prints all alphabets in reverse order
 * Takes no arguments
 *
 *Return zero when successful
 */
int main(void)
{
	char ch;

	for (ch = 'z'; ch >= 'a'; ch--)
		putchar(ch);
	putchar('\n');
	return (0);
}
