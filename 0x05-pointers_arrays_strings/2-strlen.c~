#include "holberton.h"
/**
 * _strlen - It takes one argument
 *@s: pointer on whose string lenght is to be found
 *Return: always return 0
 */
int _strlen(char *s)
{
	int len;

	len = strlen(*s);
	return (len++);
}
